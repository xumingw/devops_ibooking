<view class="tab-shell">
  <view
    wx:for="{{tabs}}"
    wx:key="key"
    class="tab-item {{active === item.key ? 'tab-item-active' : ''}}"
    data-url="{{item.url}}"
    bindtap="onTap"
  >
    <view class="tab-mark">{{item.mark}}</view>
    <text>{{item.label}}</text>
  </view>
</view>
