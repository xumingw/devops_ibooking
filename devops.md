# 自习座位预约系统的 DevOps 实践要点

---

## 1. 自动化 CI/CD 流水线

**具体场景**：
- 开发者提交代码到代码仓库后，系统自动触发构建
- 编译 前端/后端 代码，运行单元测试
- 测试通过后自动部署到 Test 环境
- 需要手动点击或审批后才部署到 Prod 环境

**在 Lab 中的体现**：
- 每次迭代的代码都能快速部署演示

---

## 2. 自动化测试

**具体场景**：
- **单元测试**：座位预约逻辑、违约判断逻辑（预约后15分钟未签到自动取消）等核心业务
- **接口测试**：预约座位API、签到API、查询历史记录API的自动化测试
- **UI 测试**：学生端Web端的功能测试（可选）

**在 Lab 中的体现**：
- 分层的，经过人工review的测试用例。
- 预约时间必须是整点小时、最多4小时、提前15分钟提醒等业务规则，都有自动化测试覆盖

---

## 3. 基础设施即代码 (IaC)

**具体场景**：
- 使用 Dockerfile 定义应用容器化
- 使用 docker-compose 本地开发环境一键启动
- 环境配置文件（数据库连接、Redis连接等）通过环境变量管理

**在 Lab 中的体现**：
- 开发组只需 `docker-compose up` 即可搭建完整的开发环境

---

## 4. 自动化部署

**具体场景**：
- GitHub Actions 一键部署到云服务器
- 每次 Build 成功自动部署到测试环境
- 生产环境部署需要审批

**在 Lab 中的体现**：
- 小组在 Review 时可以直接演示线上运行的效果

---


## 5. 环境管理

**具体场景**：
- **开发环境**：本地 Docker
- **测试环境**：GitHub Actions 自动部署到的测试服务器
- **生产环境**：正式的云服务器
- 三个环境配置通过变量管理，代码一套

---

## 6. 代码质量门禁

**具体场景**：
- 代码评审（自动化部分可选）
- 测试代码覆盖率要求 > 70%
- 单元测试不通过不能合并代码
